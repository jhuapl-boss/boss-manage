"""Downsample a volume of cubes into a single cube

Downloads cubes from S3 and uploads the results
"""

Lambda("downsampleVolumeLambda")
    """downsampleVolumeLambda

    downsample_volume.py
    """
    retry [] 1 3 1.0
